C言語メモ

開発環境　VSC  コンパイラー　MinGW デバッガーGDB

・コンパイラ言語　
    プリプロセス→高級言語→アセンブリ言語→機械語

・コンパイルオプション
    -o 実行ファイルに名前をつける
    -g 実行ファイルにデバッグ情報を付加
・プロトタイプ宣言　
    関数の型を事前にコンパイラーに知らせる。

    MSVモデル
    contolor→model,view(OK)
    view→model(BAD)

・size_t型
    符号なしの整数を格納可能
        sizeof(),malloc(),strlen()などの戻り値の格納の使う。